# robot_lego 快速恢复 — 2026-07-03

重启电脑后照这个开终端、按顺序启动即可恢复到今天的运行状态。
本文件由扫描当天实际运行进程生成(4 个终端 + 1 个 docker 视觉容器自动起)。
所有命令都在 `cd /home/i6user/Desktop/robot_lego` 下跑。

---

## 0. 每个终端通用前置
```bash
cd /home/i6user/Desktop/robot_lego
export FASTRTPS_DEFAULT_PROFILES_FILE=/home/i6user/Desktop/robot_lego/fastdds_udp_only.xml   # 禁SHM走UDP, 修 sequence-size 崩溃
source /opt/ros/humble/setup.bash
source install/setup.bash
```

---

## 1. 启动顺序(一个终端一个, 全部保持运行)

### 终端 A — UR 机器人驱动(带 rviz)
```bash
ros2 launch ur_robot_driver ur_control.launch.py ur_type:=ur5 robot_ip:=192.168.56.101 name:=ur launch_rviz:=true
```
- 机器人 IP: **192.168.56.101**
- ⚠️⚠️ 启动后**必须**在示教器打开 `externalControl.urp` 按 **▶ Play**, 否则机械臂收到 URScript 也不动。
  - 今天就因为这个"机械臂不动"排查过。验证: `robot_program_running` 应为 true。
  - 快速自检: `safety_mode=1`(正常) / `robot_mode=7`(RUNNING) / `robot_program_running=true`。

### 终端 B — O3P 相机节点(先 source SDK 自带 ros_ws)
```bash
source "/home/i6user/Desktop/robot_lego/O3P_API-0.14.0.2441.Raft_v0.14_20260623-LINUX-x86-64Bit/examples/ros_ws/install/setup.bash"
ros2 launch o3p_node camera_node.launch.py
```
- 连上打印 `Connected to camera with S/N: 455E4FDD77CAE191`

### 终端 C — O3P 抓帧 grabber(视觉帧全靠它, rate 15)
```bash
bash run_o3p_grabber.sh
```
- 写 color/color_hi/depth/frame.txt 到 `src/panda_pick/src/o3p/`(= docker `/shared_data/o3p`)

### 终端 D — 实时看相机画面(可选)
```bash
ros2 run rqt_image_view rqt_image_view /camera/frame_rgb
```

### docker 视觉容器 vision_node_final
- **不用手动起**, 跑 pipeline 时 `ensure_vision_service` 会自动 `docker start`。
- ⚠️ 千万别 `docker cp` bridge.py 进去: `/vision_code` 是 bind mount, cp 源=目标会把 bridge.py 截成 0 字节(踩过坑)。

---

## 2. 跑装配 / 拆卸

### 装配(所有 run 脚本运动速度今天已减速一半)
```bash
bash run_carrot_onrobot.sh        # 黄2x2+黄2x2+绿2x2 (3块)
bash run_estop_onrobot.sh         # 黄2x4底+红2x2顶 (2块)
bash run_burger_onrobot.sh        # 黄2x2底+黄2x4中+红/蓝2x2并列+绿2x2顶 (5块, 已转90°朝向)
bash run_trafficlight_onrobot.sh  # 绿2x2底+黄2x2中+红2x2顶 (3块)
bash run_small_tree_onrobot.sh    # 黄2x2底+绿2x4中+绿2x2顶 (3块)
```

### 拆卸(carrot / estop 已分离, 各用各的 task 文件)
```bash
bash run_carrot_disassemble.sh    # 3块, 用 disassembly_task.yaml
bash run_estop_disassemble.sh     # 2块, 用 disassembly_task_estop.yaml
GRIPPER_MODE=none bash run_carrot_disassemble.sh   # 只验轨迹不动夹爪
```

---

## 3. 今天(2026-07-03)做了什么

- **装配调参**: 抓取 descend->0.1983; check_offset/task_max_xy 安全上限 0.25->0.5; 所有装配 run 脚本运动速度+加速度减速一半(place-descend 慢速段/wiggle 未动)。
- **新产品**: burger(5块, 已90°旋转)、trafficlight、small_tree + 对应 run 脚本。
- **拆卸 carrot/estop 彻底分离**: 各自 task 文件(disassembly_task.yaml / disassembly_task_estop.yaml), 经 arm_pull 的 TASK_FILE 切换。
- **拆卸前置动作参数化**: 前置下降抓 `PRESTEP_DESCEND`(carrot 0.143/estop 0.165); 拔取点 base-x 整体平移 `PULL_OFFSET_X`(默认 +0.005, 两产品共用, 影响前置横移落地+释放+每块抓取)。
- **全代码中文注释**: cpp 装配(executeUrscriptPtpPipeline)/拆卸(executeDisassemblePickPlacePipeline)主流程 + python 总控/bridge。
- **已全部 commit + push** 到 github.com/ma-haha-hehe/robot_korea(含子模块)。

---

## 4. 排查口诀(今天用到的)

- **机械臂不动** → 先查 `robot_program_running`; 多半是示教器 External Control 没在 ▶ Play。safety_mode=1、robot_mode=7 但程序没跑 = 去按 Play。
- **ros2 命令报 XML-RPC/traceback** → ros2 daemon 卡了: `ros2 daemon stop && ros2 daemon start`。
- **git add 报 color_hi.png.tmp 消失** → grabber 在写临时文件撞车: 用 `git add -u` + 显式加新文件, 避开 o3p 的 .tmp。
- **「旧照片」** → 相机节点卡死(frame.txt 会骗人): `cd src/panda_pick/src/o3p && md5sum color.png; sleep 1; md5sum color.png` 两次一样=卡了, 重启终端 B。
- **装配别跑 gripper_flap.py** → 会把 OnRobot RG2 带偏, 需重上电。

---

## 5. 关键参数改哪(免翻代码)
- 装配抓/放深度: `src/my_robot_vision/config/vision_execution_bridge.yaml` 的 `pick.descend` / `place.descend`(免 build)。
- 装配速度: 各 `run_*_onrobot.sh` 的 `--movej/movel/...-velocity`(免 build)。
- 拆卸抓/放深度: `disassembly_task.yaml`(carrot) / `disassembly_task_estop.yaml`(estop)(免 build)。
- 拆卸前置/拔取偏移: `disassembly_arm_pull.sh`(PRESTEP_DESCEND/PULL_OFFSET_X)(免 build)。
- 动作流程本身: `src/panda_pick/src/cpp_pick_node_ur5.cpp`(要 `colcon build --packages-select panda_pick`)。
```
