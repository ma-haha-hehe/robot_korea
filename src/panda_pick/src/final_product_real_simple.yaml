# Minimal real-robot validation product.
#
# Coordinates here are placement offsets relative to
# placement.assembly_origin_base_xyz in vision_execution_bridge.yaml.
# Keep the first layer at z=0.0; the bridge already adds the calibrated
# placement height offset.

blocks:
  - name: "blue_4x2_01"
    type: "brick_4x2"
    color: "blue"
    vision_label: "blue 4x2 brick."
    pos: [0.080, 0.000, 0.000]
    rotation: [0, 0, 0]

  - name: "white_4x2_01"
    type: "brick_4x2"
    color: "white"
    vision_label: "white 4x2 brick."
    pos: [0.000, 0.000, 0.000]
    rotation: [0, 0, 0]
