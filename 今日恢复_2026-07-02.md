# robot_lego 快速恢复 — 2026-07-02

明天直接照这个开终端、按顺序启动即可恢复到今天的状态。
所有命令都在 `cd /home/i6user/Desktop/robot_lego` 下跑；每个终端先 source ROS。

---

## 0. 每个终端通用前置
```bash
cd /home/i6user/Desktop/robot_lego
export FASTRTPS_DEFAULT_PROFILES_FILE=/home/i6user/Desktop/robot_lego/fastdds_udp_only.xml   # 禁SHM走UDP, 修 sequence-size 崩溃
source /opt/ros/humble/setup.bash
source install/setup.bash
```

---

## 1. 启动顺序（一个终端一个，全部保持运行）

### 终端 A — UR 机器人驱动
```bash
ros2 launch ur_robot_driver ur_control.launch.py ur_type:=ur5 robot_ip:=192.168.56.101 name:=ur launch_rviz:=true
```
- 机器人 IP: **192.168.56.101**
- ⚠️ 启动后必须在**示教器**上打开 `externalControl.urp` 并按 **▶ Play**，否则机器人不执行任何 URScript。
  验证：`ros2 topic echo --once /io_and_status_controller/robot_program_running` 应为 `data: true`。

### 终端 B — O3P 相机节点（要先 source SDK 自带 ros_ws）
```bash
source "/home/i6user/Desktop/robot_lego/O3P_API-0.14.0.2441.Raft_v0.14_20260623-LINUX-x86-64Bit/examples/ros_ws/install/setup.bash"
ros2 launch o3p_node camera_node.launch.py
```
- 连上会打印 `Connected to camera with S/N: 455E4FDD77CAE191`

### 终端 C — O3P 抓帧 grabber（视觉帧全靠它）
```bash
bash run_o3p_grabber.sh
```
- 写 color/color_hi/depth/frame.txt 到 `src/panda_pick/src/o3p/`（= docker `/shared_data/o3p`）

### 终端 D — 实时看相机画面（可选）
```bash
ros2 run rqt_image_view rqt_image_view /camera/frame_rgb
```

### docker 视觉容器
- 名字 `vision_node_final`，**不用手动起**，跑 pipeline 时 `ensure_vision_service` 会自动 `docker start` 并 `docker cp` 桥进去。

---

## 2. 跑装配 / 拆卸 / 夹爪

### 装配（组装）
```bash
bash run_carrot_onrobot.sh    # carrot: 黄2x2+黄2x2+绿2x2 (3块), onrobot_io 夹爪
bash run_estop_onrobot.sh     # estop:  黄2x4底+红2x2顶 (2块)
```

### 拆卸（今天新建，独立于装配）
```bash
bash run_carrot_disassemble.sh   # 3块
bash run_estop_disassemble.sh    # 2块
GRIPPER_MODE=none bash run_carrot_disassemble.sh   # 只验轨迹不动夹爪
```
拆卸时序：movej 到**高处接近点** → 开爪 → 下降 → 抓 → 抬 → movej 到 observe → 横移 base +X → 下降 → 松 → 回接近点 → 下一块。
- 抓取点固定，只有放置点沿 base +X 递变（第1块0 / 第2块+8 / 第3块+16cm）。

### 夹爪单独控制（OnRobot RG2）
```bash
python3 gripper.py close      # / open / activate / width 55
```
- ⚠️ **别在装配流程里跑 `gripper_flap.py`**（RG协议，会把夹爪状态带偏，要重上电复位）。

---

## 3. 今天做了什么（改动摘要）

- **新建整套拆卸链路**：`run_disassembly.py`/`disassembly_arm_pull.sh`/`disassembly_task.yaml` + 产品脚本 `run_{carrot,estop}_disassemble.sh`。
  - cpp `executeDisassemblePickPlacePipeline` 加了 **onrobot_io** 夹爪支持 + 允许 pick descend=0。
- **拆卸示教关节角**（`src/panda_pick/config/ur5_taught_joints.env`）：
  - `DIS_PULL_TOP_JOINTS` = `DIS_HOME_JOINTS` = **高处接近点** `3.182129, -1.013967, -1.868234, -1.822239, 1.572813, -0.761432`
  - `DIS_DROP_JOINTS` = 装配 observe 点 `0.253100, -1.521235, -1.448371, -1.740353, 1.575723, -1.368144`
- **拆卸偏移**（`disassembly_task.yaml`）：pick descend **0.111**（接近点下方）、place descend **0.15**（observe 下方），`DROP_STEP_X=0.08`。
- **装配调参**（`src/my_robot_vision/config/vision_execution_bridge.yaml`）：pick descend **0.204**、place descend **0.371**、place slow_final_descend 0.01。
- **相机今天卡死过一次**：重启相机节点（终端 B）已修复。
- **`keep_gripper_closed`**：加过又按要求去掉了。装配脚本现**不带** `--keep-gripper-closed`，放置正常张开（=原行为）。代码里开关还在但默认关闭、不生效。

---

## 4. 踩过的坑 & 排查口诀

### 「旧照片」——别只信 frame.txt（会骗人）
相机节点卡死时进程还活着、frame.txt 照涨、时间戳照新，但**图像内容冻结**（grabber 把最后一张反复重存）。
- 判断真假：`cd src/panda_pick/src/o3p && md5sum color.png; sleep 1; md5sum color.png` —— 两次一样=相机卡了。
- 修：杀掉相机节点，重跑终端 B（记得先 source SDK ros_ws）。

### 夹爪「到位置就松开 / 抓不住」
今天根因：跑了 `gripper_flap.py`（RG协议）把 RG2 从数字I/O抓取状态带偏。
- 修：给夹爪**重上电复位**（示教器把工具电压关再开），已验证恢复。
- 预防：装配流程别跑 flap。

### conda 抢了 python
`python3` 指向 conda 的 3.13 → `ModuleNotFoundError: rclpy._rclpy_pybind11`。
- 用 `/usr/bin/python3`（3.10，rclpy 对应版本），或 `conda deactivate`。

### 机器人不动
多半 `robot_program_running: false` → 示教器上 External Control 程序没在 Play。去按 ▶。

---

## 5. 相关记忆
详见 `项目状态交接.md` 和 auto-memory（`装配/拆卸互不混用`、`相机O3P启动命令`、`夹爪OnRobot RG2` 等）。
