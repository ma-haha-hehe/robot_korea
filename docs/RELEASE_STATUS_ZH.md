# 发布状态与已知限制

记录时间：2026-09-24。此版本提供 Workbenchmark 导入、0°/90°逆向抓取规划、Oracle 机械臂执行、塑料接触模型及实际开爪确认。

## 已完成验证

- 自动测试：221 项通过，1 项跳过。
- 原始 Workbenchmark 任务：36 项完整 Oracle 物理执行通过，seed=42，基座 X=-0.05 m。包括30项第一层级任务、2项第二层级任务和4项第三层级任务。
- 每个通过任务均核对实际进程零退出、逐块末态、接触穿透、坐实支撑、双指实测全开并脱离，以及最终全体积木唤醒后的1秒稳定性。
- 塑料模型包括柔顺内筋和摩擦扣合，无运行时焊接或积木位姿传送。轴向夹具中的2×2/2×4拔出力约为3.0/5.8 N；这是未做实物标定的仿真结果。

[机器可读证据](validation/workbenchmark_plastic_20260924.json)包含实际结果、末态、动作计划、源文件哈希及严格计划复用依据。只有动作完全相同且控制器、接触模型不变的旧运行才能复用；更改动作的任务需要重新执行。

## 尚未通过

| 任务 | 最近完整运行结果 | 当前状态 |
| --- | --- | --- |
| tier3_task_079 | 第八块抓取或运输期间掉落，后续位姿补偿发生 IK 失败 | 完整接触追踪中 |
| tier4_task_051 | 第九块在松爪后漂移，终止压合 | 改为有完整底面支撑的高位落位顺序，物理复验中 |
| tier4_task_001 | 3600秒计算预算耗尽；当时11块已达到目标，但撤离与最后一块未完成 | 超时失败，未计通过 |

以上是已记录任务的结果，不能外推为400项全部通过。固定240项抽样仅定义回归输入，尚未执行完毕。运行中的任务、初始化末态的静置诊断、历史snap结果均不计入36项。

GroundingDINO/SAM/FoundationPose接口可选，但真实GPU推理仍未验收；实机操作未验收。运行方法见[README](../README.md)，塑料参数和判定门槛见[接触模型说明](PLASTIC_CONTACT_ZH.md)。
