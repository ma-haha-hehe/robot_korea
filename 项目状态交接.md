# 项目状态交接(robot_lego / UR5 + O3P 视觉抓放)

> **下次打开 Claude,先读这个文件,就能接上全部上下文。** 最后更新: 2026-06-27。
> 一句话: 相机已从 RealSense 换成 ifm O3P,手眼+各种坐标变换基本调通,**抓取/放置位置和朝向基本对了,只剩"抓取点 ~8mm 基座系残差"待用真值标定收尾**。

---

## 0. 当前在干什么(最重要,接着干这个)
**抓取点还偏积木后方约 8mm(≈1 stud),是基座系系统残差**(用户确认: FoundationPose 中心准、夹爪指尖在法兰正下方,所以不是 mesh中心 也不是 指尖偏移问题 → 是相机→基座变换链的残差)。

**收尾办法 = 真值标定(还没做):**
1. 跑一次抓取(回 observe→拍→抓),抓完积木和机械臂都别动
2. 用户手动把夹爪移到积木**几何中心**正上方,说"好了"
3. Claude 读: 正确中心 TCP、这一帧 FoundationPose 物体位置、绿轴方向 → 误差(基座系向量)→ 设进 `pick.pick_offset_xyz`
4. 再挪积木到**另一个位置**重测: 误差一样=固定偏移(pick_offset 搞定); 不一样=`observe_T_base_tool` 旋转有微残差(要重测 observe 矩阵)

读 wrist/位姿的方法: `ros2 topic echo --once /joint_states` 和 `ros2 launch panda_pick run_ur5.launch.py motion_control_mode:=print_pose`。

---

## 1. 跑视觉/抓放需要的进程("三件套" + docker + 机器人)
| 进程 | 启动 | 现在状态 |
|---|---|---|
| ① O3P 相机 ROS 节点 | `ros2 launch o3p_node camera_node.launch.py`(需 source O3P工作区) | 在跑 PID 1564350 |
| ② grabber(把ROS话题→o3p/文件) | `bash run_o3p_grabber.sh` | 在跑 PID 1678859 |
| ③ Docker 视觉桥(FoundationPose) | 容器 `vision_node_final`(`sudo docker start vision_node_final`) | 需 sudo 查 |
| ④ UR5 机器人驱动 | 用户终端 | 间歇性(见 gotcha) |

**"旧照片"故障 = grabber 没跑 或 相机节点卡死**。判断: `src/panda_pick/src/o3p/color_hi.png` 的 md5 和 `frame.txt` 要持续变。只动计数器不动图 = 相机节点卡死(重启相机节点)。

## 2. 怎么跑一次抓放
用户的运行脚本: `run_pick.sh` 等(`run_*.sh`,装配类)。核心是 `run_auto_pick_pipeline.py`。
- 只跑视觉不动机械臂: `--skip-observe --dry-run`
- pipeline 顺序: 回observe → 拍 → 视觉桥出 `T_cam_obj` → `vision_to_execution_yaml.py` 换算成 `pick_place_task.yaml` → 机械臂执行
- ⚠️ **视觉必须在 observe 位拍**(pipeline 会自动先回 observe),否则 `observe_T_base_tool` 对不上、算出的 base 系朝向/位置乱跳。

## 3. 当前关键配置(`src/my_robot_vision/config/vision_execution_bridge.yaml`)
```
pick_pre_base_xyz:        [0.242646, 0.541928, 0.154775]   # pregrasp TCP, 已实测, 别动(用户明确)
place_pre_base_xyz:       [-0.1254, 0.3891, 0.3051]         # preplace TCP, 估计值(待精确重测)
assembly_origin_base_xyz: [-0.1254, 0.3891, 0.3051]         # 装配中心(用户手摆), 落点=origin+place_plan+place_offset
flip_x / flip_y:          true                              # base_link↔base 差180°, 必须true
use_vision_yaw:           true                              # 用 FoundationPose 朝向
use_planner_grasp_spin:   false                             # 绿轴已直接对长边, 不叠加90
pick_yaw_offset_deg:      -46.02                            # 配 "绿轴取负" 用(两点标定)
place_yaw_offset_deg:     5.5                               # place 夹爪摆正
long_axis_offset_m:       0.0                               # 对象系长轴偏移(暂不用)
pick.pick_offset_xyz:     [0.0, 0.0, 0.050]                 # 基座系抓取微调(xy待真值标)
place_offset_xyz:         [0.033, 0.03046, 0.080]           # Y+ = 往前
pick.descend:             0.129   place.descend: 0.2115
```
夹爪闭合速度: `run_*.sh` 里 `--gripper-speed 50`(Robotiq SPE, 越小越慢)。

**手眼**: `src/my_robot_vision/config/camera_to_tool.yaml` 的 `T_tool_camera`(O3P重标, 平移[55.6,-11.4,25.9]mm, std 1-3mm)。

## 4. 已经解决的(排查历程)
- **相机换 O3P** + 重做手眼标定(新工具链 `calib_*_o3p`, 鱼眼+组合回深度系+4×6棋盘自动纠180°翻转)。
- **镜像/对角大bug的根因 = base vs base_link 差180°**(执行端 `cpp_pick_node_ur5.cpp` 在URScript base系加dx/dy, 换算脚本算的是base_link)→ `flip_x/y=true` 补偿(我曾误关导致镜像)。
- **过期示教位姿**: `observe_T_base_tool`、`pick_pre_base_xyz` 都用当前 observe/print_pose 实测重写(UR5_OBSERVE/PREGRASP_JOINTS 改过没跟着更新)。
- **yaw 调通**: 夹爪平行 FoundationPose **绿轴(Y)**=抓长边; 代码取**绿轴并取负**(两点标定实测斜率≈-1, 符号反); `normalize_lego_yaw_deg` 让手腕走 [-90,90] **最短路径**(杜绝315°绕远)。
- pick 位置居中、place 位置/朝向/高度、夹爪速度 都按用户反馈调了。

## 5. 还没做完 / 待办
1. ⏳ **抓取 ~8mm 基座系残差**(见第0节,真值标定)。
2. ⏳ `place_pre_base_xyz` 是估计值 → 机械臂精确停到 preplace 后 print_pose 重测, 让 place 更准。
3. ⏳ **yaw 符号跨角度验证**: 转两个明显不同角度的积木, 确认夹爪都跟着对(目前两点标定误差~2°)。
4. ⏳ place 落点用了估计的 place_ref, 跑一次看准不准, 差常数就移 assembly_origin。

## 6. 必须知道的坑(Gotchas)
- **/joint_states 相对 URScript 命令有符号翻转**: 命令 wrist_3=-0.974 → /joint_states 显示 +0.974(关节1-4,6取反, 关节5不变)。算 wrist 相关都要注意。
- **base vs base_link 差 180°(绕Z)**: `tf2_echo base base_link` 验证过。tool0_controller(base)的x,y 是 print_pose(base_link)的相反数。
- **机械臂 movej 间歇不执行**: 有时命令了关节但机械臂没动(日志"静止检测未满足"超时, wrist 没变)。怀疑跟远程模式/保护停止有关。让机械臂去某姿态后**务必读 /joint_states 确认真到位**。
- **视觉 yaw "乱跳"是假象**: 之前看到绿轴角帧间跳, 后确认是**积木被移动了** + **非observe位拍的帧**(observe_T_base_tool对不上)。FoundationPose 本身在 observe 位拍是稳的。

## 7. 关键文件
| 文件 | 作用 |
|---|---|
| `src/my_robot_vision/config/vision_execution_bridge.yaml` | **换算总配置**(上面所有值) |
| `src/my_robot_vision/config/camera_to_tool.yaml` | 手眼外参 T_tool_camera |
| `src/my_robot_vision/my_robot_vision/vision_to_execution_yaml.py` | T_cam_obj → pick_place_task.yaml 换算(yaw/flip/offset 都在这) |
| `src/my_robot_vision/my_robot_vision/run_auto_pick_pipeline.py` | host端总控 pipeline |
| `src/panda_pick/src/cpp_pick_node_ur5.cpp` | 执行端(movej到示教位+movel加dx/dy, URScript base系) |
| `src/my_robot_vision/my_robot_vision/o3p_grabber.py` + `run_o3p_grabber.sh` | grabber |
| `src/my_robot_vision/my_robot_vision/o3p_align.py` | 鱼眼: 高清mask投影回深度帧 |
| `vision_node_test1_5_bridge.py`(容器内) | 视觉桥: 检测→投影→FoundationPose→T_cam_obj |
| `calib_capture_o3p.sh`/`calib_run_o3p.sh`/`*_o3p.py` | O3P 手眼标定工具链 |
| `legacy_realsense_handeye/` | 旧 RealSense+写死手眼 备份(含 README 说明) |
| `src/panda_pick/config/ur5_taught_joints.env` | 示教关节(OBSERVE/PREGRASP 现在都是 -1.81 底座, 即同一位姿) |

## 8. O3P 视觉数据流(简版)
相机每帧给3图: `color_hi.png`(高清鱼眼,检测用) / `color.png`(对齐彩色240×180,SDK已对齐深度) / `depth.npy`。
检测在**高清图**做(GroundingDINO+SAM)→ mask 用 `o3p_align` 鱼眼投影回**深度帧** → FoundationPose(深度帧 rgb+depth+mask)出 `T_cam_obj`(**深度光学系**)。所以手眼/朝向都基于深度系。

## 9. Git
远程 `git@github.com:ma-haha-hehe/robot_korea.git` 分支 main。最新提交 `75ad080`。大模型权重等被 .gitignore 排除。
