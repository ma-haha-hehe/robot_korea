# Pipeline 全貌 + 2026-06-30 改动记录（UR5 + O3P 视觉抓放）

> 回头读这个就能接上。配套老文件: `项目状态交接.md`（更早的背景/坑）。
> 本文 = 当前 pipeline 数据流 + 今天这一轮所有改动 + 待办。

---

## 0. 一张图还是每次重拍？（重要）

**用 `run_pick.sh`（带 `--async-next-vision`）= 冻结一张图，全程不重拍。**
- 开头机器人回 observe 位，拍**一张** RGB-D 帧 → **冻结**（`frozen_observation=True`）。
- 之后 5 块积木的位姿**全部从这同一张冻结帧算**（用 `used_region` 排除已抓区域）。
- 好处: 机器人在抓 A 的同时，可并行从冻结帧算 B。
- 含义: **开跑前所有积木必须已摆好、在视野内**；中途加积木不会被看到。

若**不**加 `--async-next-vision`（另一条路径）= `frozen_observation=False`，则每轮回 observe 重拍。当前 run_pick.sh 是冻结模式。

---

## 1. 全链路（数据怎么传）

```
[O3P相机ROS节点] ──话题──> [grabber] ──写文件──> [o3p/=容器/shared_data] ──> [视觉桥(docker)]
                                                                              │ T_cam_obj
                                                                              ▼
[UR5] <──URScript── [cpp执行节点] <──pick_place_task.yaml── [换算脚本] <── vision_output.yaml
   ▲                                                                          
   └─────────────── run_auto_pick_pipeline.py 总控 ───────────────────────────┘
```

### ① 相机取数（ROS话题 → 文件）
相机节点 `o3p_node`（在 SDK 自带 `O3P_API-*/examples/ros_ws`，**需先 source 那个 ws**）发布:
- `/camera/frame_rgb` 高清鱼眼 1344×1008（检测用）
- `/camera/aligned_depth_to_color/image_raw` 对齐彩色 240×180
- `/camera/depth/image_raw` 深度 240×180 mm
- `/camera/*camera_info` 内参

`o3p_grabber.py`（15Hz）订阅上面话题，写到 `src/panda_pick/src/o3p/`:
`color_hi.png` / `color.png` / `depth.npy` / `camera_params.json` / `frame.txt`
→ 这个目录 = 容器内 `/shared_data`（docker 挂载）。**grabber 不跑 = 旧照片故障**（看 frame.txt 是否在变）。

### ② 视觉推理（容器 vision_node_final，出 T_cam_obj）
`vision_node_test1_5_bridge.py`（pipeline 每次 docker cp 最新源码再启动）:
1. 等 `/shared_data/current_target.txt`（总控写目标，如 "white 2x4 brick."）
2. 冻结 observe 帧
3. 高清图上 GroundingDINO+SAM 检测 → mask（"Detection Logic"窗）
4. `o3p_align` 鱼眼把 mask 投影回深度帧
5. FoundationPose → `T_cam_obj`（深度光学系）（"6D Pose Tracking"窗）
6. 写 `vision_output.yaml`
- 窗口开关: `--show-windows` / 环境变量 `VISION_SHOW_WINDOWS`。

### ③ 换算 + 执行
换算 `vision_to_execution_yaml.py`（host, 纯Python, 无ROS）:
```
T_base_obj = observe_T_base_tool · T_tool_camera · T_cam_obj
抓取点 = T_base_obj + long_axis_offset(0) + pick_offset_xyz
dx/dy  = (抓取点 − pick_pre_base_xyz) → flip_x/y 转 URScript base 系
yaw    = -绿轴方位角 + pick_yaw_offset, 归一[-90,90]
```
→ `pick_place_task.yaml`（小偏移）。
执行 `cpp_pick_node_ur5.cpp`（URScript 发到 `/urscript_interface/script_command`）:
`movej pregrasp → movel dx/dy → 降 pick.descend 抓 → 抬 → movej preplace → 降 place.descend 放(wiggle) → 抬 → 回 UR5_HOME`。
总控 `run_auto_pick_pipeline.py`: 回observe→触发视觉→等输出→换算→执行→下一块。

---

## 2. 关键文件
| 文件 | 作用 |
|---|---|
| `src/my_robot_vision/config/vision_execution_bridge.yaml` | 换算总配置（下面所有值） |
| `src/my_robot_vision/config/camera_to_tool.yaml` | 手眼外参 T_tool_camera |
| `src/panda_pick/config/ur5_taught_joints.env` | 示教关节(OBSERVE/HOME/PREGRASP/PREPLACE) |
| `vision_to_execution_yaml.py` | T_cam_obj→pick_place_task 换算 |
| `run_auto_pick_pipeline.py` | host 总控 |
| `cpp_pick_node_ur5.cpp` | 执行端 |
| `vision_node_test1_5_bridge.py` | 容器内视觉桥 |
| `o3p_grabber.py` / `run_o3p_grabber.sh` | grabber |
| `run_pick.sh` | 跑一次抓放(当前默认脚本) |

---

## 3. 2026-06-30 这一轮改了什么

### 位姿（observe/home 拆分 + pregrasp/preplace 重设）
`ur5_taught_joints.env`:
- `UR5_OBSERVE_JOINTS = -1.071338, -1.319376, -1.796090, -1.592703, 1.542375, -1.877391`（拍照位, 高位俯视）
- `UR5_HOME_JOINTS = -0.635688, -1.250922, -1.840651, -1.629104, 1.543189, -1.441650`（执行后返回位, **新加的独立变量**）
- `UR5_PREGRASP_JOINTS = =OBSERVE`（方案A: pregrasp 直接用 observe, 因为它正对积木）
- `UR5_PREPLACE_JOINTS = 0.116749, -1.424336, -1.706625, -1.606441, 1.556330, -0.689083`

`vision_execution_bridge.yaml`:
- `observe_T_base_tool` = observe 位实测 4×4（平移 [-0.0848, 0.3882, 0.4291]）
- `pick_pre_base_xyz = [-0.084805, 0.388211, 0.429149]`（=observe TCP, 配合方案A）
- `place_pre_base_xyz = [-0.432748, 0.060326, 0.429157]`（新preplace实测）
- `assembly_origin_base_xyz = [-0.432748, 0.060326, 0.429157]`（**临时=preplace, 待用真装配原点重设**）

### 抓取标定 / 朝向 / 高度
- `pick_yaw_offset_deg: -46.02 → 43.98`（+90°: 之前抓短边, 改抓长边）
- `pick.pick_offset_xyz: [0,0,0.05] → [-0.0116,-0.0012,0.05]`
  - **两点真值标定**: P1误差[-10.6,-0.5]mm, P2[-12.5,-1.8]mm, 两位置+不同朝向几乎一致
    → 确认是**恒定平移误差**（相机正装无倾角, 旋转残差极小）→ 取平均补 xy。
- `pick.descend: 0.129 → 0.165`（累计低 3.6cm; 深度仍在调）
- `place.descend: 0.2115 → 0.1915`（放置高 2cm）

### 工程/运行
- `install/` 之前是 6/19 旧副本 → **重新 `colcon build --symlink-install`**, 现在 install 软链到 src，改源码立即生效；并 `chmod +x` 了 run_auto_pick_pipeline.py（否则 "No executable found"）。
- `run_pick.sh`: 加 `--show-windows`（弹视觉窗口）; **取消两段开合夹爪**（移除 --gripper-pregrasp-position 128 / --gripper-release-position 120，恢复方法写在脚本末尾注释）。
- 视觉桥 imshow 全部收进 `SHOW_DEBUG_WINDOWS`(env `VISION_SHOW_WINDOWS`) 开关。

---

## 4. 待办 / 下一步
1. **深度(z)**: descend 仍是估计, 用户自行用 `pick.descend`/`place.descend` 调准。
2. **验证抓长边**: 跑一次确认 +90° 后夹爪夹在长边(2-stud 宽合拢); 不对再 ±90°。
3. **验证 xy 标定**: 跑抓取看是否正中中心; 残余几 mm 再叠进 pick_offset_xyz。
4. **assembly_origin**: 现临时=preplace, 跑放置看落点偏多少, 用真装配原点重设。
5. **place_pre 精度**: 机械臂精确停 preplace 后可再 print_pose 校。

---

## 5. 关键命令
```bash
# 跑一次抓放(当前默认):
cd ~/Desktop/robot_lego && bash run_pick.sh          # 中途 sudo 输 i6user

# 只验证视觉/换算不动机械臂:
ros2 run my_robot_vision run_auto_pick_pipeline.py \
  --product-yaml src/panda_pick/src/final_product_orange_green_blue_white_simple.yaml \
  --start-index 0 --max-tasks 1 --dry-run --gripper-mode robotiq_socket --gripper-speed 50 --vision-timeout 180

# 三件套(视觉栈):
#  ① 相机: source O3P_API-*/examples/ros_ws/install/setup.bash && ros2 launch o3p_node camera_node.launch.py
#  ② grabber: bash run_o3p_grabber.sh
#  ③ docker: sudo docker start vision_node_final
#  ④ 示教器: Remote 模式 + External Control 程序 Play (否则 movej 不执行)

# 读位姿(机器人到位后, 别动):
ros2 launch panda_pick run_ur5.launch.py motion_control_mode:=print_joints   # 关节(原样填env)
ros2 launch panda_pick run_ur5.launch.py motion_control_mode:=print_pose     # TCP(base_link, 填矩阵/xyz)

# 移动到某示教位(joint_ptp, 改 pregrasp_joints 即可):
ros2 launch panda_pick run_ur5.launch.py motion_control_mode:=joint_ptp gripper_control_mode:=none \
  urscript_pregrasp_joints:="$UR5_OBSERVE_JOINTS" urscript_home_joints:="$UR5_OBSERVE_JOINTS" \
  joint_ptp_return_home:=false urscript_pipeline_wait_seconds:=8.0 \
  urscript_movej_velocity:=0.90 urscript_movej_acceleration:=2.0
# 注意: 先 source /opt/ros/humble + install/setup.bash + ur5_taught_joints.env, 否则变量空/包找不到
```

## 6. 当前 product（5块, `final_product_orange_green_blue_white_simple.yaml`）
底层2白2x4并排 → 绿2x4转90°横跨 → 蓝2x4 → 顶橙2x2。每层 z+0.0192。
落点 = assembly_origin + 各块 pos + place_offset_xyz。
