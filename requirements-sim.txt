# Python-only simulation dependencies. ROS packages are installed by
# install_sim_system_deps.sh because ROS Python messages are not PyPI packages.
# Versions used for the recorded assembly validation.
mujoco==3.11.0
trimesh==4.12.2
